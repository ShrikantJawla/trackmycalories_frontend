import { FOLLOW_REQ } from "./connectAndFollow.types"


const initialState = {

}

export const connectAndFollowReducer = (state = initialState, { type, payload }) => {
    switch (type) {
        case '':

            break;

        default:
            return state;
    }
}